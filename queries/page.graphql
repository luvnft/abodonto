query Page($slug: String!) {
  pagina(where: { slug: $slug }) {
    hero {
      titulo
      texto
      image {
        url
      }
      buttons {
        text
        url
        variant
      }
    }
  }
}
